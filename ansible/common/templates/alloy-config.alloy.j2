// Journald log shipping to Loki
loki.relabel "journal" {
  forward_to = []

  rule {
    source_labels = ["__journal__systemd_unit"]
    target_label  = "unit"
  }

  rule {
    source_labels = ["__journal__transport"]
    target_label  = "transport"
  }
}

loki.source.journal "read" {
  forward_to    = [loki.write.loki.receiver]
  relabel_rules = loki.relabel.journal.rules
  labels        = { source = "journald", host = "{{ monitoring_hostname }}" }
}

loki.write "loki" {
  endpoint {
    // The url carries a trailing dot (rooted FQDN) — see loki_push_url in
    // ansible/common/monitoring.yml. THAT DOT is the real fix for the
    // recurring "prom dark / 421 Unknown host" stall: it stops Go from
    // search-suffixing `loki.ablz.au` → `loki.ablz.au.ablz.au` → caddy's
    // `*.ablz.au` wildcard (192.168.1.6), which 421s every push. The old
    // `enable_http2 = false` here was a misdiagnosis (blamed HTTP/2
    // coalescing — but it was deployed and prom STILL 421'd) and was
    // removed 2026-06-19. Full RCA: docs/wiki/services/lgtm-stack.md.
    url = "{{ loki_push_url }}"
    // doc2 (Loki host) auto-reboots nightly for ~10-15 min. Default
    // max_backoff_period=5m exhausted retries at ~9 min and dropped
    // a batch on 2026-05-21. Bump so the in-memory queue rides
    // normal maintenance windows; WAL covers anything longer.
    max_backoff_period = "10m"
  }

  // Persist queued batches to /var/lib/alloy/data so we do not lose
  // logs when Loki is briefly down OR when alloy itself restarts.
  // WAL is off by default in alloy; prometheus.remote_write to Mimir
  // already has its own on-disk WAL, so this closes the symmetric
  // Loki-side gap. Mirrors modules/nixos/services/loki.nix.
  wal {
    enabled         = true
    max_segment_age = "2h"
  }
}

// Scrape node-exporter metrics
prometheus.scrape "node" {
  targets = [{
    __address__ = "localhost:9100",
    instance    = "{{ monitoring_hostname }}",
  }]
  forward_to      = [prometheus.remote_write.mimir.receiver]
  scrape_interval = "60s"
  job_name        = "node"
}

// Push metrics to Mimir
prometheus.remote_write "mimir" {
  endpoint {
    // Rooted FQDN (trailing dot) — same fix as loki.write above; see
    // mimir_push_url in ansible/common/monitoring.yml. (Mimir never hit
    // the 421 in practice because it re-dialed cleanly, but it carries the
    // identical search-suffix exposure, so it gets the identical fix.)
    url = "{{ mimir_push_url }}"
  }
  external_labels = {
    host = "{{ monitoring_hostname }}",
  }
}
