./docker/immich/.env
.env*
changed.txt

# OpenTofu/Terraform state (managed by terranix)
vms/tofu/.state/
nix/vms/tofu/.state/
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
tfplan

# Nix build outputs and local caches
result
.cache/
ha/esphome/

# Episodic memory (conversation archives with potential secrets)
docs/episodic-memory*
