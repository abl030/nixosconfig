# Commissioning status and recovery notes:
# docs/wiki/services/indoor-water-meter.md
substitutions:
  # WMMJH-25-PN standard pulse option: 1 reed closure per 10 litres.
  # This is configurable because the same meter family also offers a 1 L/pulse
  # option. The value means "reed closures per litre" and must be > 0.
  pulses_per_litre: "0.1"

esphome:
  name: indoor-water-meter
  friendly_name: Indoor Water Meter
  min_version: 2026.7.2
  on_boot:
    priority: -100
    then:
      - lambda: |-
          // Data schema 1 removes the one known synthetic pulse created by
          // shorting the field wires during commissioning on 2026-07-27.
          if (id(water_meter_data_schema) < 1) {
            if (id(lifetime_water_pulses) != 0) {
              id(lifetime_water_pulses) -= 1;
              ESP_LOGI(
                "water_meter",
                "Removed one synthetic commissioning pulse"
              );
            }
            id(water_meter_data_schema) = 1;
          }

          ESP_LOGI(
            "water_meter",
            "Restored lifetime pulse count: %llu",
            static_cast<unsigned long long>(id(lifetime_water_pulses))
          );
      # Allow the restored globals component to queue both migration values,
      # then force that one-time change to NVS before commissioning continues.
      - delay: 2s
      - lambda: global_preferences->sync();

esp32:
  board: esp32dev
  framework:
    type: esp-idf

# Preference changes are coalesced in RAM and flushed to flash at most once per
# minute. ESPHome also syncs them during a graceful reboot/OTA shutdown. A hard
# power loss can therefore lose no more than roughly the last minute of pulses.
preferences:
  flash_write_interval: 1min

logger:
  level: INFO

api:
  encryption:
    key: !secret water_meter_api_encryption_key

ota:
  - platform: esphome
    password: !secret water_meter_ota_password

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "Water-Meter-Recovery"
    password: !secret water_meter_fallback_ap_password

captive_portal:

globals:
  # pulse_meter's own total is session-only. Keep the authoritative raw pulse
  # count in ESP32 NVS so calibration can change without losing the raw count.
  - id: lifetime_water_pulses
    type: uint64_t
    restore_value: true
    initial_value: "0"
    update_interval: 1s
  # One-time persisted-data migrations. Schema 1 removes the known synthetic
  # commissioning pulse without adding a permanent offset to water maths.
  - id: water_meter_data_schema
    type: uint8_t
    restore_value: true
    initial_value: "0"
    update_interval: 1s

sensor:
  - platform: pulse_meter
    id: water_pulse_meter
    name: "Flow Rate"
    pin:
      number: GPIO27
      allow_other_uses: true
      mode:
        input: true
        pullup: true
      inverted: true
    # PULSE mode requires a stable pulse, making it suitable for a mechanical
    # reed that may bounce on both closure and release.
    internal_filter: 10ms
    internal_filter_mode: PULSE
    timeout: 2min
    unit_of_measurement: "L/min"
    device_class: volume_flow_rate
    state_class: measurement
    accuracy_decimals: 3
    filters:
      - lambda: return x / ${pulses_per_litre};
    total:
      id: session_water_pulses
      internal: true
      on_value:
        then:
          - lambda: |-
              static uint32_t last_session_pulses = 0;
              const uint32_t session_pulses = static_cast<uint32_t>(x);
              const uint32_t new_pulses =
                session_pulses - last_session_pulses;
              last_session_pulses = session_pulses;

              if (new_pulses != 0) {
                id(lifetime_water_pulses) += new_pulses;
                ESP_LOGI(
                  "water_meter",
                  "Counted %u pulse(s); lifetime total: %llu",
                  static_cast<unsigned int>(new_pulses),
                  static_cast<unsigned long long>(id(lifetime_water_pulses))
                );
              }

  # Exposing the restored raw count makes pulse verification independent of the
  # volume conversion.
  - platform: template
    id: lifetime_pulses_sensor
    name: "Lifetime Pulses"
    lambda: return static_cast<float>(id(lifetime_water_pulses));
    update_interval: 5s
    unit_of_measurement: "pulses"
    state_class: total_increasing
    accuracy_decimals: 0
    entity_category: diagnostic

  - platform: template
    id: total_water_litres
    name: "Total Water Litres"
    lambda: |-
      return static_cast<float>(
        static_cast<double>(id(lifetime_water_pulses)) /
        static_cast<double>(${pulses_per_litre})
      );
    update_interval: 5s
    unit_of_measurement: "L"
    device_class: water
    state_class: total_increasing
    accuracy_decimals: 3

  - platform: template
    id: total_water_cubic_metres
    name: "Total Water"
    lambda: |-
      return static_cast<float>(
        static_cast<double>(id(lifetime_water_pulses)) /
        static_cast<double>(${pulses_per_litre}) /
        1000.0
      );
    update_interval: 5s
    unit_of_measurement: "m³"
    device_class: water
    state_class: total_increasing
    accuracy_decimals: 6

binary_sensor:
  # Direct electrical view of the reed input. This is intentionally diagnostic:
  # ON means GPIO27 is being pulled to GND, independent of pulse conversion.
  - platform: gpio
    id: reed_contact
    name: "Reed Contact"
    pin:
      number: GPIO27
      allow_other_uses: true
      mode:
        input: true
        pullup: true
      inverted: true
    use_interrupt: false
    filters:
      - delayed_on_off: 10ms
    entity_category: diagnostic
