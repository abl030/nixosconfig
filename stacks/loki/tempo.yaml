server:
  http_listen_port: 3200

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
        http:

ingester:
  max_block_duration: 5m

storage:
  trace:
    backend: local
    local:
      path: /data/tempo
    wal:
      path: /data/tempo/wal
