# Install as /etc/systemd/system/pve-container@111.service.d/kopia-mounts.conf.
# A failed host mount must stop CT startup, never expose an empty source tree.
[Unit]
RequiresMountsFor=/mnt/mum-ct /nvmeprom/backup/pfsense-ct /mnt/kopia-sources/music /mnt/kopia-sources/ali-cratedigger
