# LLM patch prompt: remaining Nix lints

_Generated: 2025-10-08 12:49:05 UTC_

Autofixes done with:
  • deadnix --edit
  • statix fix

For each file below, the **lint findings** are listed first, then the **full file**.

Return **ONLY full file replacements** (no diffs). For each file, output exactly:
----- BEGIN FILE <path> -----
```nix
<complete file contents>
```
----- END FILE <path> -----

Constraints: make the **smallest** changes needed so that:
  • `deadnix` (no flags) reports zero unused declarations
  • `statix check` (default) reports zero lints
Preserve comments, semantics, and style; avoid churn.

================================================================
FILE: error: Found argument 'modules/nixos/ci/test.nix' which wasn't expected, or isn't valid in this context
----------------------------------------------------------------
Statix (errfmt):
  (no statix findings)

Deadnix (context):
  (no deadnix findings)

----- BEGIN FILE error: Found argument 'modules/nixos/ci/test.nix' which wasn't expected, or isn't valid in this context -----
```nix
```
----- END FILE error: Found argument 'modules/nixos/ci/test.nix' which wasn't expected, or isn't valid in this context -----

================================================================
FILE: For more information try --help
----------------------------------------------------------------
Statix (errfmt):
  (no statix findings)

Deadnix (context):
  (no deadnix findings)

----- BEGIN FILE For more information try --help -----
```nix
```
----- END FILE For more information try --help -----

================================================================
FILE:     statix check [OPTIONS] [--] [TARGET]
----------------------------------------------------------------
Statix (errfmt):
  (no statix findings)

Deadnix (context):
  (no deadnix findings)

----- BEGIN FILE     statix check [OPTIONS] [--] [TARGET] -----
```nix
```
----- END FILE     statix check [OPTIONS] [--] [TARGET] -----

================================================================
FILE: USAGE:
----------------------------------------------------------------
Statix (errfmt):
  (no statix findings)

Deadnix (context):
  (no deadnix findings)

----- BEGIN FILE USAGE: -----
```nix
```
----- END FILE USAGE: -----
